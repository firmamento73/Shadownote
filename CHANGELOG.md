# 更新日志

本项目的所有显著改动都记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.1] - 2026-08-07

这一版做两件事：让**工作台的一键生成真正可用**（此前长逐字稿几乎必然失败），以及解决「填好了 key，追问却打不开，也不知道为什么」。

### 修复 · 工作台生成

- **整篇逐字稿被塞进一次请求**：切块函数判定「时间戳行」的条件是*整行短于 48 字*，而清理后的逐字稿是 `[mm:ss] 正文……` 一行一段、几乎必然超过 48 字，于是一个分段点都找不到，整篇合成一块发出去——长课必然超上下文或被截断。现改为**优先匹配行首时间戳**，并保留「短行含时间戳」的判定以兼容通义听悟原始导出（`发言人1 00:00:12` 独占一行）两种形态；另加字符数兜底，即使完全认不出时间戳也不会整篇发出。
- **`.json` 笔记的 `raw` 字段全空**：原文切片用的是同一套失效判定，且只收*时间戳行之后*的行，而清理后的正文就在时间戳同一行上——两处叠加导致导出的 JSON 里每段 `raw` 都是空的，追问模式虽然开着却拿不到任何原文。现已修正，并把两处判定合并为同一个函数，避免再次不一致。
- **中途一次限流就前功尽弃**：任何一块请求返回 429 / 5xx 或网络抖动，整轮生成直接失败、已完成的部分也不保留。现对可重试错误做**指数退避重试**（最多 3 次），进度条上会显示重试状态。
- **输出被静默截断**：不少服务商默认输出上限为 4096 token，清理步骤的输出长度≈输入长度，很容易超限后被无声截断，留下半句话。现在检测 `finish_reason === 'length'`，**自动把该块对半拆开重跑**；同时按步骤区分块大小（清理 4000 字 / 生成 8000 字），从源头降低触发概率。

### 修复 · 追问

- **载入 `.json` 笔记后目录里不出现「问」字**：`qaEnabled` 在 `applySegments()` 之后才置位，导致首次渲染目录时追问入口被跳过，看起来就像 JSON 笔记没生效。现在先置位再渲染。
- **macOS / Safari 直接双击打开本地文件时配置丢失**：Safari 对 `file://` 页面禁用 `localStorage`，此前写入失败被静默吞掉，用户填完 key 一刷新就没了、且毫无提示。现在启动时主动探测（`LS_OK`），不可用时在模型设置区显著提示，并说明改用 Chrome / Edge 普通窗口打开。
- **在模型设置里填了 Base URL / Key / 模型却不生效**：清理 / 生成 / 问答三个阶段各自记忆预设，若它们指向的是另一套空预设，填好的那套不会被用上。现在保存或失焦时会把仍指向空预设的阶段自动切到这套填全的预设。

### 新增

- **生成后自检**：自动检查模型是不是编了时间戳、时间戳有没有超出视频总时长、有没有重复段、有没有整个跑偏没按格式输出，并把问题直接写在状态栏，不再等你载入后才发现不对。
- **分块提示**：逐字稿被切成多块时，自动在提示词前加一句说明当前是第几块，避免模型每块都写一遍开场白和总结。该说明由程序附加，不会改动你在「提示词」里编辑保存的内容。
- **「自检」按钮**：一键汇总浏览器存储可用性、当前笔记类型与段数、各预设的填写情况、问答选用的预设、能否启动追问及其原因、页面打开方式与 UA，并复制到剪贴板，方便直接贴给作者排查。
- **追问打不开时给出具体原因**：不再只弹一个通用帮助页，而是明确说明卡在哪一步——笔记不是 JSON、缺 Base URL / Key / 模型名，还会检测出**哪一套预设是填全的**并提示切换过去。诊断信息以高亮气泡置顶显示，同时打开预设下拉，可当场切换。
- **当前段落卡片下方新增「✦ 就这段问 AI」按钮**：此前进入追问只能右键目录或点目录里的小「问」字，入口不易发现。

### 改进

- 内置「清理」提示词更新为最新版：强调结合语境和上下文修改，并明确覆盖术语误听之外的其它语音识别错误。
- 载入笔记后的状态提示补充了下一步操作：「（追问模式 · 点目录里的「问」或右上角「问答」开始）」。
- 生成完成后状态栏会显示实际生成了多少段。
- 模型设置的三个输入框现在失焦即自动保存到当前预设，不再依赖手动点「保存到此预设」。

### 文档

- 新增 [`docs/TUTORIAL.md`](docs/TUTORIAL.md)：从视频到 `notes.json` 的完整制作教程（含截图），涵盖工作台自动生成与纯手工两条路线，另附原始 Word 版。
- 仓库内的 `随影笔记 .html` 更名为 `随影笔记.html`（此前文件名多了一个空格，与 README 中的说明不一致）。

## [1.0] - 2026

首个公开版本。

[1.1]: https://github.com/firmamento73/ShadowNote/releases/tag/v1.1
[1.0]: https://github.com/firmamento73/ShadowNote/releases/tag/v1.0
