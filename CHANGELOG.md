# 更新日志

本项目的所有显著改动都记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.1] - 2026-08-07

这一版集中解决「填好了 key，追问却打不开，也不知道为什么」的问题，并修复了 macOS / Safari 下配置丢失的情况。

### 修复

- **载入 `.json` 笔记后目录里不出现「问」字**：`qaEnabled` 在 `applySegments()` 之后才置位，导致首次渲染目录时追问入口被跳过，看起来就像 JSON 笔记没生效。现在先置位再渲染。
- **macOS / Safari 直接双击打开本地文件时配置丢失**：Safari 对 `file://` 页面禁用 `localStorage`，此前写入失败被静默吞掉，用户填完 key 一刷新就没了、且毫无提示。现在启动时主动探测（`LS_OK`），不可用时在模型设置区显著提示，并说明改用 Chrome / Edge 普通窗口打开。
- **在模型设置里填了 Base URL / Key / 模型却不生效**：清理 / 生成 / 问答三个阶段各自记忆预设，若它们指向的是另一套空预设，填好的那套不会被用上。现在保存或失焦时会把仍指向空预设的阶段自动切到这套填全的预设（`syncStageSels`）。

### 新增

- **「自检」按钮**：一键汇总浏览器存储可用性、当前笔记类型与段数、各预设的填写情况、问答选用的预设、能否启动追问及其原因、页面打开方式与 UA，并复制到剪贴板，方便直接贴给作者排查。
- **`qaBlockReason()` 诊断说明**：追问打不开时不再只弹一个通用帮助页，而是明确说明卡在哪一步——笔记不是 JSON、缺 Base URL / Key / 模型名，还会检测出**哪一套预设是填全的**并提示切换过去。诊断信息以高亮气泡置顶显示，同时打开预设下拉，可当场切换。
- **当前段落卡片下方新增「✦ 就这段问 AI」按钮**：此前进入追问只能右键目录或点目录里的小「问」字，入口不易发现。

### 改进

- 载入笔记后的状态提示补充了下一步操作：「（追问模式 · 点目录里的「问」或右上角「问答」开始）」。
- 模型设置的三个输入框现在失焦即自动保存到当前预设，不再依赖手动点「保存到此预设」。

### 文档

- 新增 [`docs/TUTORIAL.md`](docs/TUTORIAL.md)：从视频到 `notes.json` 的完整三步制作教程（含截图），另附原始 Word 版。
- 仓库内的 `随影笔记 .html` 更名为 `随影笔记.html`（此前文件名多了一个空格，与 README 中的说明不一致）。

## [1.0] - 2026

首个公开版本。

[1.1]: https://github.com/firmamento73/ShadowNote/releases/tag/v1.1
[1.0]: https://github.com/firmamento73/ShadowNote/releases/tag/v1.0
